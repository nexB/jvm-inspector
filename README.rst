jvm-inspector
=============

jvm-inspector is a set of utilities to inspect source and binary files used with a JVM such as Java (tm) and to collect interesting data from them. This is also a ScanCode-toolkit plugin.
